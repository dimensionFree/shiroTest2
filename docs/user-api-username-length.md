# 用户名长度变更说明

## 背景
- 前端当前未对用户名长度做限制。
- 后端注册接口需承担兜底校验，避免写入超长用户名导致数据库异常。

## 变更内容
- 数据库：`user.username` 列长度由 `varchar(10)` 调整为 `varchar(255)`（见 Flyway `V18`）。
- 后端校验：注册接口用户名长度上限由 10 调整为 255，超出返回统一错误结构。

## 兼容性
- 该变更仅放宽长度限制，不影响已有用户名数据读取。
